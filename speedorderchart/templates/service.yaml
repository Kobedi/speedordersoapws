apiVersion: v1
kind: Service
metadata:
  name: {{ include "speedorderchart.fullname" . }}
  labels:
    {{- include "speedorderchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "speedorderchart.selectorLabels" . | nindent 4 }}
