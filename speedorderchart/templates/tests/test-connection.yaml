apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "speedorderchart.fullname" . }}-test-connection"
  labels:
    {{- include "speedorderchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "speedorderchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
